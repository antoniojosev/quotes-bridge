# Quotes Bridge

A Laravel package that integrates with the DummyJSON Quotes API. Provides non-blocking rate limiting, O(log n) cache retrieval over a sorted array, a resilient batch-import command, and a publishable Vue 3 + TypeScript UI.

> Documentation pending. Final README ships with the deliverable.
